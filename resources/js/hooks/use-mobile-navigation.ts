/**
 * Starter-kit compatibility hook.
 * In this template we keep it as a safe no-op cleanup callback.
 */
export function useMobileNavigation() {
    return () => {
        // Intentionally empty.
    };
}
